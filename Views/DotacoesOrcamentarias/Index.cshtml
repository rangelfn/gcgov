@model IEnumerable<gcgov.Models.DotacaoOrcamentaria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FonteRecurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgramaTrabalho)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FonteRecurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramaTrabalho)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NaturezaDespesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NaturezaDespesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NaturezaDespesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
