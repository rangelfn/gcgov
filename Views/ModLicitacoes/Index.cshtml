@model IEnumerable<gcgov.Models.ModLicitacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModLicitacaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModLicitacaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModLicitacaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
