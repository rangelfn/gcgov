@model IEnumerable<GCGov.Models.PgtosModalidade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-labeled btn-primary" title="Cadastrar">
        <span class="btn-label"><i class="fa-regular fa-square-plus"></i></span>Cadastrar
    </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PgtoModNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PgtoModNome)
            </td>
            <td>
                <a asp-action="Details" class="btn btn-secondary" asp-route-id="@item.PgtoModId"  title="Ver Detalhes"><i class="fas fa-file-alt"></i></a>
                <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.PgtoModId" title="Editar"><i class="fas fa-pen-square"></i></a>
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.PgtoModId" title="Apagar"><i class="fas fa-trash"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
